#!/usr/bin/env bash

set -euo pipefail

if [[ ! $# -ge 1 ]]; then
  echo "usage is $0 <day number> <optional: year>"
  exit 1
fi

day=$1
year=${2:-2025}

dir="${REPO_DIR}/${year}/${day}"

aocd "${day}" "${year}" > "${dir}/input.txt"
